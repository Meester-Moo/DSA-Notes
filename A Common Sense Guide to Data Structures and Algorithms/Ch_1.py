my_array = ["apples", "bananas", "cucumbers", "dates", "elderberries"]

my_array[2]								#my_array[i] returns the element at index i

my_array.index("dates")		#my_array.index(element) returns the index

my_array.insert(2, "cantaloupe")		#my_array.insert(i, element) inserts the element at index i

my_array[3]		#Read	-instant 

my_array["dates"]		#Search (N)

my_set = {'a', 'b', 'c'}  #Sets can never contain a duplicate value